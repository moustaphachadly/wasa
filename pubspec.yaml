name: wasa
description: Mini games for kids.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.3 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  wave: ^0.2.2
#  just_audio: ^0.9.28
  collection: ^1.16.0
  audioplayers: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/piano_notes/
    - assets/images/games/